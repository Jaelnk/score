<form method="post" action="{{ path('app_tpersona_delete', {'idPersona': tpersona.idPersona}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tpersona.idPersona) }}">
    <button class="btn">Delete</button>
</form>
